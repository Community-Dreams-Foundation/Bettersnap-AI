import { Mail } from 'lucide-react'
import { useState } from 'react'
import { useAuth } from '../../auth'
import { authConfigured } from '../../lib/auth'
import { useRouter } from '../../lib/router'

export function LoginPage(){const{navigate}=useRouter();const auth=useAuth();const[loading,setLoading]=useState(false);const[error,setError]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError('');try{await auth.login();navigate('/dashboard')}catch(e){setError(e instanceof Error?e.message:'Sign-in failed')}finally{setLoading(false)}};return <main className="login-page"><div className="login-visual"><div className="login-brand"><div className="brand-mark">B</div>BetterSnap<span>.AI</span></div><div><p className="eyebrow">SUPER ADMIN CONSOLE</p><h1>Clarity and control,<br/>at every scale.</h1><p>Monitor your platform, support your customers, and keep BetterSnap running beautifully.</p></div><small>© 2026 BetterSnap.AI. Internal use only.</small></div><section className="login-panel"><form className="login-card" onSubmit={submit}><div className="mobile-login-brand"><div className="brand-mark">B</div>BetterSnap.AI</div><h2>Super Admin sign in</h2><p>Continue with the authorized Microsoft account <strong>sivmm29@gmail.com</strong>.</p>{error&&<div className="inline-error" role="alert">{error}</div>}{!authConfigured&&<div className="inline-error">Microsoft Entra configuration is missing. Add the VITE_ENTRA values to your .env file.</div>}<button className="button login-submit" disabled={loading||!authConfigured}><Mail/>{loading?'Signing in...':'Continue with Microsoft'}</button><p className="login-help">Access is restricted to the configured BetterSnap Admin account.</p></form></section></main>}
