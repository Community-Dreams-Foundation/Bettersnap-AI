import { AlertTriangle } from 'lucide-react'
import { EmptyState, PageHeader } from '../../components'
import { DataSourceBadge } from '../dashboard/DashboardPage'

const descriptions:Record<string,string>={payments:'Platform payment transactions and controlled refund requests need SuperAdmin payment endpoints.',subscriptions:'Platform-wide subscription records, active counts, cancellation, and reactivation need SuperAdmin subscription endpoints.',credits:'A platform credit ledger and immutable adjustment endpoint are not available yet. User detail includes a read-only per-user ledger.','audit-logs':'Admin audit-log read and write endpoints are not available yet.'}
export function BackendUnavailablePage({resource}:{resource:string}){const title=resource.split('-').map(x=>x[0].toUpperCase()+x.slice(1)).join(' ');return <><PageHeader title={title} description="This SuperAdmin capability has not been implemented in the backend." action={<DataSourceBadge type="required"/>}/><div className="notice-banner"><AlertTriangle size={18}/><div className="notice-content"><strong>Not available yet</strong><p>No records are displayed until an actual SuperAdmin endpoint is available.</p></div></div><section className="card"><EmptyState title="Backend endpoint required" description={descriptions[resource]||'A role-protected SuperAdmin endpoint is required.'}/></section></>}
