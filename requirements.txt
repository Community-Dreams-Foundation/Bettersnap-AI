sentencepiece
# Bumped 0.35.0 -> 0.36.0 to test the FluxKontext denoising-integration bug
# (genuine weights + clean VAE but velocity~0 / static output). diffusers is the
# ONLY intentionally-changed variable: transformers + torch were cleared by the
# std-trajectory data, and accelerate is pinned to the version the failing run
# used (1.14.0) so the bump is the sole difference.
diffusers==0.36.0
transformers==4.49.0
accelerate==1.14.0
huggingface_hub>=0.34.0
Pillow
requests
azure-storage-queue
azure-storage-blob
azure-identity
azure-keyvault-secrets
pyodbc
python-dotenv
protobuf